# Sprint report — 2026-08-20 (sprint 5)

```
Repository: C:/Users/sghar/CascadeProjects/relay
Isolation:  worktree ../relay-sprint-2026-08-20-5 on sprint/2026-08-20-5
Main checkout at start: 1 file dirty (ROADMAP.md) — not mine, untouched
```

> 🔴 **READ THIS BEFORE MERGING.** The one dirty file in the main checkout is
> `ROADMAP.md` — the revision-3 rewrite produced immediately before this sprint, **uncommitted and
> tracked by nothing**. Every commit on this branch references its sprint 1, and the version of
> `ROADMAP.md` *on this branch* is the older one from `72cb21b`, because a worktree is checked out
> from HEAD. I did not touch it: it is a human's uncommitted work and committing it from a sprint is
> the failure mode §0.0 of the sprint rules exists to prevent.
>
> **Commit `ROADMAP.md` from the main checkout, alongside or before merging this branch.** Iteration
> 3 exists precisely because those findings were living in that one uncommitted file.

## 1. Backlog source

`sprint 1 now` → **ROADMAP.md §3 Sprint 1, "Make the claim true again"**, read from the main
checkout (read-only). Not inferred.

Sprint 1 as written has five items. **Four of them cannot be executed from a worktree**, and that is
not a limitation to work around — it is the isolation working:

| item | why it could not run here |
|---|---|
| 1.1 owner's vault holds real items | needs production credentials **and** real content only Steve has |
| 1.2 name a recipient and a verifier | same, plus an owner session |
| 1.3 real release configuration | credentialed |
| 1.5 `verify:orphans` after the walk | `--env-file=.env.local`, absent by design |
| **1.4 re-stamp the ladder** | **doable, and taken as iteration 1** |

Gitignored env files do not come with a worktree, and the rules forbid copying secrets in to make
them run. So the sprint was re-scoped, per §4's allowed "reduce to a thinner vertical slice that is
still genuinely complete", to: **make the repo's claim about itself true and checkable, and convert
the four blocked items from something Steve must remember into something he can run.**

## 2. Baseline

| | at start | at end |
|---|---|---|
| types (`tsc --noEmit`) | ✅ | ✅ |
| lint (`eslint . --max-warnings=0`) | ✅ | ✅ |
| test (`vitest --run`) | ✅ 267 files / 2984 | ✅ 269 files / 3033 |
| build (`next build`) | ❌ → ✅ | ✅ |
| coverage | not evaluated by anything | ✅ enforced in CI — 91.46 / 85.19 / 91.02 / 92.7 |

**The red build was an isolation artifact, not a repo defect, and is recorded as orientation rather
than iteration 1.** `TurbopackInternalError: Symlink [project]/node_modules is invalid` — the
`node_modules` junction the sprint rules prescribe breaks Turbopack, a lesson already recorded in
this portfolio's memory. Fixed as that lesson prescribes: `cmd //c rmdir` the junction first (never
`rm -rf`, which follows the link into the shared `node_modules`), then `npm ci` in the worktree.
Shared `node_modules` verified intact at 417 entries afterwards. Types, lint and tests were green
throughout — only the build stage was affected.

## 3. Shipped

### I1 — the ladder claim argues for itself · axis: correctness · `53ef5bf`

**Acceptance criteria (written first):** a pure verdict rejecting an unknown rung, missing evidence,
an absent/malformed/**future** `as_of`, a missing `basis`/`record`, a `record` naming a file that is
not there, and an aged claim with no `scope`; each proven by a planted violation; plus a meta-check
for the parser going blind. All met.

`PROJECT.yaml` said `ladder: dogfooded` beneath four successive prose caveats — 2026-06-27, 08-07,
08-08, 08-09 — each narrowing what the claim covered, every one enforced by nothing. Production on
2026-08-20: one owner account, zero vault items, zero recipients, zero verifiers, no release that
had ever left `armed`.

**Proving test:** `lib/ops/ladder-claim.test.ts`, 20 cases. Verified red-then-green in the intended
order — 18 planted-violation cases passed while the 2 real-file cases failed for want of a
`ladder_evidence` block, then all 20 passed once the honest record was written. `as_of: '2026-08-08'`
(the ten-journey production walk), with a `scope` stating plainly that the rung describes a
demonstration and not ongoing use.

### I2 — `verify:dogfood` · axis: wiring · `f799db8`

**Acceptance criteria:** a read-only probe reporting the production counts against the criteria that
make a cohort invitation meaningful, naming the concrete next action for each missing piece; not
satisfiable by demo fixtures; distinguishing "not ready" from "could not run"; wired as an npm
script. All met.

`scripts/invite-cohort.ts` invites people to stand by **for the owner's vault**. Nothing checked
whether that vault could host them. The access-rule count is the criterion that matters most — items
and people with no rule between them are two lists, which looks populated in the UI.

**Proving test:** `lib/ops/dogfood-readiness.test.ts`. The read-only guarantee was proven by planting
`DELETE FROM users` and watching the assertion name the verb; script restored byte-identical by
checksum. Exit 1 = not ready, exit 2 = could not run — collapsing those is how a broken probe reads
as a clean result.

⚠️ **The repo caught my omission**: `lib/ops/verify-scripts-documented.test.ts` failed the gate
because the new `verify:*` script was undocumented. CLAUDE.md now documents it.

### I3 — seven findings become durable · axis: completeness · `d84a7e5`

**Acceptance criteria:** seven entries in `PROJECT.yaml → deferred`, each with an owner, an
`opened` date and an `ends_when` that is a condition rather than a date, each self-contained enough
to survive without `ROADMAP.md`. All met — register went 18 → 25 entries.

`A0` vault-is-empty · `D8` coverage-gate-unenforced · `D9` csp-report-sink-expires · `D10`
journey-sweep-stale · `D11` secret-ages-unknown · `D12` auth/react-major-behind · `E1'`
lapse-notice-wired-not-live-proven.

Two are filed to **prevent** work: D12 records that upgrading `next-auth` v4 / React 18 is barred by
the Infrastructure Change Policy absent a documented problem, so the next reviewer finds the decision
instead of opening a migration; A0 records that seeding with `reset-demo.ts` is not an acceptable
close.

### I4 — the coverage thresholds are read by something · axis: correctness · `241995a`

**Acceptance criteria:** CI evaluates the declared thresholds on every push; proven to fail by a
planted violation; the declared thresholds themselves unchanged. All met.

`vitest.config.ts` has declared 80/80/70/80 since it was written and no pipeline evaluated them. The
CI file's own header already made the argument — *"a pipeline that only checks syntax on a repo whose
value is correctness is a pipeline that lies."*

**Proving test:** branches raised 70 → 99 against a real 85.2; `npm run test:coverage` exited 1
naming the threshold; `vitest.config.ts` restored and verified byte-identical by checksum. The
workflow YAML was parsed after editing — a broken workflow silently disables CI, which is the
false-green shape this repo keeps meeting.

### I5 — `invite:cohort` refuses an empty vault · axis: security · `a2886e1`

**Acceptance criteria:** `--commit` refuses when the vault cannot host an invitation, naming what is
missing; the **dry run is never gated**; an override exists and prints what it overrides; the gate is
placed after the dry-run return and before any person is created; one shared definition of the
counts. All met.

I2 produced a report, and a report is a convention somebody must remember to run. The people in
`.relay-cohort.json` are real; withdrawing an invitation costs a conversation.

**Proving test:** four wiring assertions, verified by planting a removal of the gate call.

⚠️ **Two of those four passed their own plant before being tightened**, and both are recorded in the
commit because both are recurrences of shapes this repo has already been bitten by.
`toContain('gateCohortInvite')` matched the surviving *import* after the call was deleted — the
"Stripe inside NotStripe" shape. And the ordering assertion compared an `indexOf` without bounding
it, so a deleted gate scored `-1` and was dutifully "less than" the creation index: **the test passed
most convincingly when the guard was gone entirely.** Re-planted after tightening: 4/4 catch it.

## 4. Blocked

### BLOCKED: S1-1.1 / 1.2 / 1.3 — populate the owner's vault
**Category:** credentials + decision needed (content only Steve has)
**Goal:** real vault items, a named recipient and verifier, an access rule joining them, and a
configured release trigger — so the cohort has something to be invited into.
**Stopped at:** no `.env.local` in a worktree by design; and the *content* is real personal
credentials, which no agent should invent.
**Attempt 1:** re-scoped to what is executable in isolation — the ladder record (I1).
**Attempt 2:** built the instrument instead (I2), then made the prerequisite structural (I5), so the
work is now one command to check and impossible to skip past.
**Need from you:** sign in at relaystandby.com and populate the vault, then run
`npm run verify:dogfood` from the main checkout until it reads READY.
**Est. unblock cost:** 20–30 minutes.
**Downstream impact:** blocks the beta cohort (A3), which blocks Phase 0 claim conversion — the
number two shipped security decisions rest on.
**Partial work:** none left behind; the instrument and the refusal are shipped and independent.

### BLOCKED: S1-1.5 — `verify:orphans` after the walk
**Category:** credentials
**Stopped at:** `npx tsx --env-file=.env.local scripts/disposable-sweep.ts` — file absent in a worktree.
**Need from you:** run it from the main checkout after any walk day. Unchanged by this sprint.
**Est. unblock cost:** 2 minutes.

### Verification this sprint could NOT run, and you should
- `npm run verify:dogfood` — **never run against a real cluster.** Smoke-run only: without
  `.env.local` it exits 2 at the connection, which proves it loads and its imports resolve. Its SQL
  has never met the database.
- `npm run invite:cohort` past its input check — the refusal path has unit and wiring proof, and has
  never executed against live counts.
- `npm run gate` on this branch used the worktree's fresh `npm ci`, not the main checkout's tree.

## 5. Deferred queue, scored, ready for the next run

| item | axis | score | note |
|---|---|---|---|
| D10 journey sweep re-walk | correctness | (5×3)/2 = 7.5 | needs credentials + `npm run dev`; J3/J6/J9 also need a `verify:live` decision |
| D9 CSP report sink | security | (5×2)/1 = 10 | **needs a decision from Steve**, not code: persist the reports or enforce `script-src` |
| E1' lapse-notice live proof | correctness | (5×2)/1 = 10 | Steve fires a test-mode event, then **re-delivers it** |
| D11 secret ages | security | (5×2)/1 = 10 | AWS key age is CLI-discoverable; the rest need Steve |
| B5 KMS encryption context | security | (5×3)/3 = 5 | design done, build deferred by ruling — do not start unprompted |
| A0 populate the vault | journeys | — | Steve; gates the cohort |

## 6. Debt created

- **`verify:dogfood`'s SQL is unproven against a real cluster.** Filed as the "verification you should
  run" list in §4 rather than as a register entry, because one credentialed run closes it.
- **`--anyway` has no recorded-reason requirement.** It prints what it overrides but does not capture
  *why* in any durable place. Deliberate: a free-text prompt in a script is friction that gets pasted
  through. If the override is ever used, the reason belongs in the register.
- **This branch's `ROADMAP.md` is the pre-revision-3 version** (see the banner at the top). Not debt I
  can clear from a worktree.

## 7. Recommended top 3 for the next sprint

1. **Commit `ROADMAP.md` from the main checkout, then populate the vault (A0).** The first is one
   command and protects a document that currently exists in one uncommitted place. The second is
   20 minutes and unblocks the entire demand lane — which, per revision 3's own §7, did not move at
   all across the four sprints before this one.
2. **The two Steve-court proofs that are each under ten minutes: E1' and D11.** E1' has a date behind
   it — when `beta-free-release` is revisited on 2026-10-01 and the paywall flips, an expired card
   becomes a blocked release, and the notification standing between those has never met Stripe.
3. **D9, as a decision rather than a build.** The report-only CSP cannot accumulate evidence, so
   "keep gathering data" is not one of the available options; the choice is persist or enforce.

---

**Teardown.** The worktree is left in place for review:

```bash
git worktree remove ../relay-sprint-2026-08-20-5
git worktree prune
```

No `node_modules` junction to remove first — it was replaced with a real `npm ci` install during
orientation, so `git worktree remove` will not hit the `Permission denied` the junction causes.
