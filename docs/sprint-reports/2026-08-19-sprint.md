# Sprint report — 2026-08-19

```
Repository: C:/Users/sghar/CascadeProjects/relay
Isolation:  worktree C:/Users/sghar/CascadeProjects/relay-sprint-2026-08-19-1 on sprint/2026-08-19-1
Main checkout at start: clean (0 files dirty)   ·   at end: clean (0 files dirty)
```

## 1. Backlog source

**Explicit, named in the invocation.** The five-item "Claude list" produced by the assessment
earlier in this session, plus five pieces of Steve guidance applied to the "Steve list". The
assessment itself was derived from `PROJECT.yaml → deferred:`,
`docs/sprint-reports/2026-08-18-sprint.md` and `docs/g1-outlet-dossier.md`, all re-read live rather
than recalled. Not inferred.

Steve's guidance, and where each landed:

| # | Guidance | Landed |
|---|---|---|
| 1 | plan for 8/19 | S1 — send checklist rows 1–2 dated 2026-08-19 |
| 2 | postal address, email, phone | S1 — **gitignored**, never in the repo (see the finding below) |
| 3 | leave the threshold alone until 11/30 | S2 — recorded as a ratified decision, **with its conflict named and guarded** |
| 4 | prioritised list of orgs to target | S3 — `docs/g3-partner-dossier.md`, nine candidates, routes verified |
| 5 | defer [the cohort] for 4 days | S2 — recorded as deferred to 2026-08-23 |

## 2. Baseline

| | Start | End |
|---|---|---|
| types (`tsc --noEmit`) | green | green |
| lint (`eslint --max-warnings=0`) | green | green |
| tests | green — 248 files, 2719 passed, 1 skipped | green — **252 files, 2760 passed, 1 skipped** |
| build (`next build`) | green | green |
| `npm run gate` | **exit 0** | **exit 0** |
| suite under `TZ=UTC` | not run at baseline | **exit 0** (repo rule: CI is UTC, this laptop is not) |

⚠️ **The first baseline run failed `gate:build`, and it was my isolation rather than the repo.**
Turbopack rejects the `node_modules` junction the sprint procedure creates —
*"Symlink [project]/node_modules is invalid, it points out of the filesystem root"*. Replaced with a
real `npm ci` in the worktree (20s) and the baseline came back green. **The junction step in the
sprint procedure does not work for this repo**; worth fixing there, since a red baseline is supposed
to mean something.

## 3. Shipped

### S1 — the outlet's contact requirement, and the details that must not be published · `0e029d0` · axis: documentation

**Acceptance criteria** (written first): the checklist carries the outlet's stated contact
requirement quoted from the dossier; the values are not in the repo; `.gitignore` covers them and
`git check-ignore` confirms; a test fails if any value reaches a tracked file; rows 1–2 carry the
2026-08-19 date; gate green.

caregiver.com requires *"name address and current telephone number and email address with all
submissions"*. The 2026-08-18 dossier re-check found that and recorded it there — and the send
checklist in `oped-angle-3-draft.md` had ten rows and none of them was this one. A dossier finding
that never reaches the working document is a requirement nobody meets on the day.

🔴 **THE FINDING THAT CHANGED HOW THIS WAS DONE: `github.com/sgharlow/relay` is a PUBLIC
repository.** Committing a home address and personal phone number here publishes them permanently —
a later edit does not take them back, only a history rewrite on a public repo does. Steve supplied
them so the submission would be complete, not so they would be indexed. They live in
`.relay-submitter.json` (gitignored, following the `.relay-cohort.json` precedent for real people's
data) and every document names the **path**.

**Proof**: `lib/ops/submitter-details-stay-out-of-the-repo.test.ts`, proven in both directions with
the real values — green with the address absent, red the moment it is planted in a tracked file.
Two layers: structural (gitignored + untracked, runs in CI with no file present) and a value scan
across `git ls-files` (runs where the values exist, which is the only machine a careless
`git add -A` could publish them from), plus a positive control proving the scanner reads the corpus.

**Two narrowings, both found by the guard going red on correct content** — recorded because each
would otherwise look like a weakening:
- `postal_code`/`state`/`country` are short codes that collide with ordinary text. A synthetic
  `00000` matched **twelve** tracked files — the audit chain's `'0'*64` genesis hash in the schema
  docs and four auth tests. A guard that fires on the genesis hash is a guard somebody deletes.
- `name` and `email` are **already published on purpose** — LICENSE, `lib/contact.ts`,
  `src/app/about/page.tsx`, `PROJECT.yaml`. Scanning for them fails the build on the copyright line.
  What committing the file would *newly* expose is a street address and a phone number.

### S2 — three deferrals get a date, and two get a guard · `39cb3b3` · axis: correctness

**Acceptance criteria**: each of the three decisions recorded in `PROJECT.yaml` with an owner and a
revisit date; the threshold deferral's conflict stated rather than left to be discovered; a
`ratify_by` past-due check exists and is proven to fail; the ordering rule is enforced structurally;
`PROJECT.yaml` still parses as YAML; gate green.

🔴 **The conflict in guidance #3, named rather than quietly absorbed.** The thresholds exist under a
rule older than the deferral: they must be on record *before the first placement goes live*, "so the
number cannot be reverse-engineered from whichever result appears". A placement plausibly lands in
October, ahead of 11-30 — and whoever ratified afterwards would be choosing a bar with the result
already visible, which is not a bar. Reconciled the one way that keeps both: **a threshold may be
deferred; a lane may not be declared without one.** `editorial-preflight-claims.test.ts` fails if
any `ed-*` src enters `GATE_LANES` while the deferral stands. **Pitching, drafting and submitting
all stay green** — only counting is gated.

🔴 **The gap the PROPOSED-gate guard closes**, found by reading `PROJECT.yaml` against itself.
`g3-b2b2c-pilot-loi` claims *"if this is still unratified on 2026-11-30, lib/ops/gates.test.ts fails
the suite and forces the decision."* True — and **only** on 11-30, because `due:` was the only date
the file read. Its own `ratify_by: 2026-09-01` bought nothing, so a PROPOSED gate could sit
unanswered for three months with every check green. That is the gates section's own complaint from
the other side: *"A dead precondition does not stop work; it stops work being SCHEDULED, silently,
which is worse."*

**Proof** — four planted violations, all red: `ratify_by` moved into the past → names g3;
`ratify_by` removed from a PROPOSED gate → "declares no ratify_by"; `ed-caregiver-com` added to
`GATE_LANES` → names the slug; `PLANNED_EDITORIAL_SRCS` emptied → "the guard can never fire".

Also corrected: `docs/user-journeys.md` "what is genuinely open" item 6 still said Phase 1
`secret_kinds` was *"awaiting the migration"*. Migration 035 landed, Phase 1 was wired and
live-proven, and the prompt shipped — all on 2026-08-18, one day after that line was written.

### S3 — nine partner candidates, and an argument with the gate · `a271689` · axis: completeness

**Acceptance criteria**: every candidate's route read from the organisation's own page; routes that
could not be confirmed marked unverified rather than filled in; a prioritised order with reasoning;
the naming stays Steve's; the gate cites the dossier; gate green.

`docs/g3-partner-dossier.md`. **Two findings that argue with the gate rather than filling it in:**

1. **Three of the four categories the gate names are financial** — benefits broker, credit union,
   wealth manager — inherited from a Relay that had an estate product. Estate was narrowed out
   permanently on 2026-08-14. A wealth manager's interest in this category *is* the inheritance
   moment, so it is now the weakest of the four and probably belongs deleted. The best-aligned
   partners are employer caregiver-benefit platforms, whose coordinators already sit with a family
   in the week Relay is for.
2. **Caregiver Action Network — the obvious name — is the one to decline for now.** Its Corporate
   Leadership Council is a **paid tiered sponsorship** (logos grouped Sustaining/Champion/Leading/
   Caring/Supporting; pricing unpublished). Paying a caregiver nonprofit for marketing exposure
   before one arms-length person has said they would pay is exactly what the demand-gate rule
   exists to stop — the same reasoning that declined a $5–15k counsel opinion on 2026-08-14.

**Recommended order: Homethrive, then Wellthy, with NAC's Innovation Collaborative in parallel.**
Homethrive is first on *behaviour*, not product fit: it has publicly integrated two partners'
capabilities in 2026 (Healthee in February; powering Bright Horizons' *Care Advising* in May). Every
other candidate is a guess about receptiveness; that one has evidence. NAC runs in parallel because
it is the only candidate with a named human and no known fee — and it is **standing, not
distribution**, which the dossier keeps separate because conflating the two is how a gate stops
meaning anything.

**Negative routes verified too**, and they matter as much: `wellthy.com/partners` **404s** (the only
route is a sales demo form); `cariloop.com/partners` publishes no program; **`n4a.org` 301-redirects
to `usaging.org`**, so any citation of n4a.org is stale by an organisation rename.

**And the defect the new guard found in its own first draft.** `gates.test.ts` now fails if a gate
cites a `docs/*.md` that is not in the repo. Planting a rename did **not** make it fail on the first
attempt: the dossier path had been line-wrapped inside a YAML string as `docs/g3-partner-` /
`dossier.md`, so it existed nowhere as a contiguous string, `grep` could not find it, and the guard
silently matched nothing. Moved to its own single-line `dossier:` key with a comment saying why it
must stay there; re-planted, and it now names both the gate and the missing file.

### S4 — something finally counts the accounts the walks leave behind · `3272945` · axis: correctness

**Acceptance criteria**: counts reserved-domain accounts and reports each one's rows; exits non-zero
when any is older than 24h; never writes; an account with a subscription or a standby role elsewhere
is reported held and never sweepable; a held account can never also be counted stale; documented;
gate green.

`npm run verify:orphans`. This is the gap **D4 named and then did not close**, in its own words:
*"a separate test cluster would make the walks safe, but it would NOT make an abandoned row visible.
A count of disposable accounts older than a day, checked somewhere, is a smaller and more useful
thing than the cluster — and nothing in this repo does it."* Four accounts were found on production
on 2026-08-18 by looking, after an unrelated question. Every run that created them reported success.

**D4 stays open.** The test cluster is an infrastructure decision with a cost and is Steve's; this is
the cheap half the expensive half would not have delivered anyway.

🔴 **The defect the guard caught in the script's own first draft.** It queried `standby_user_id` — a
column that exists in no migration and in no other query. It is `claimed_user_id`. Unit tests passed,
`tsc` was clean, the build was clean, and it would have thrown on its first real run against
production. **SQL is invisible to the type checker**, which is exactly why a script that only ever
runs with credentials needs a guard that runs without them. That guard is anchored to
`lifecycle.ts` rather than to a literal, so renaming the column in both places passes and renaming
it in one fails.

A second trap on the guard's own first run: the negative grep matched the **comment explaining the
bug**. That has now bitten four times in this repo (api-reachability, step-up-guard, type-scale, and
this), so the check strips comments first — the same `ownVoice` idea `editorial-preflight-claims`
uses for markdown.

**Proof** — four planted violations, all red: wrong column; a `DELETE` added to the sweep; the
reserved-domain lists drifting apart between the module and `scripts/disposable-owner.ts`; the
hold/stale precedence inverted.

**An existing guard did its job on me**: `verify-scripts-documented.test.ts` failed the build for a
`verify:*` script no runbook mentioned, which is why CLAUDE.md now says when to run it.

### S5 — the live walks get a dead-man · `ff28278` · axis: correctness

**Acceptance criteria**: a stamp is written only when all five walks exit 0; the suite fails when the
newest stamp goes stale; "never run" and "corrupt" are distinct states; a future timestamp is not
treated as fresh; the stamp cannot be reordered ahead of a walk; gate green.

CLAUDE.md already conceded the weakness — *"A gate somebody has to remember is a gate on the days
they remember."* The chain now ends with `npm run verify:stamp`, appending one line to
`docs/verify-live-runs.jsonl`, so **a stamp exists only if all five walks exited 0: a side effect of
success, never a claim of it.** `verify-live-freshness.test.ts` fails when the newest stamp ages past
14 days — designed to go red on a date with no code change, exactly as `gates.test.ts` is.

Three states kept apart because collapsing them is how a monitor lies: `never`, `unreadable`
(corrupt **or dated in the future**), and `stale`. A future timestamp read as maximally fresh would
be this log's worst possible failure — the dead-man would go permanently quiet on a lie.

**Exactly one line is hand-written**: the 2026-08-18 chain run, marked `backfilled: true`, citing
`PROJECT.yaml`'s `chain_live_proven` and commit `0e2654e` as its evidence. Seeding it silently would
have been the intent-recorded-as-execution failure the whole file exists to stop.

**Proof**: the stamp script really appends (run standalone — it needs no credentials); the dead-man
fires on a 40-day-old log; and it fires on a deleted log.

**Two more existing guards caught this work, both correctly.** `gate-script` asserted `verify:live`
had exactly five `&&` links — the count is now derived from the steps present rather than bumped,
because a literal must be edited by whoever adds a walk, at the moment they are least likely to
think about it. A **new** assertion was added that the stamp is **last**: anywhere else it witnesses
a chain that had not finished, and the dead-man would read a stamp nothing earned (proven by moving
it to the front). `walks-assert-the-product` treats everything in the chain as a walk, so the
stamp's `lib` import tripped it; added to `MAY_IMPORT` with its reason rather than exempted from the
file set, because an exemption is a hole somebody later walks through.

## 4. Blocked

**None.** No item hit the blocker protocol.

## 5. Deferred queue — seeds the next run

| Item | Axis | Blast | Effort | Score | Note |
|---|---|---|---|---|---|
| **D2** the remaining requirable factors (sms, email, passkey, hardware_key, security_questions) | completeness 4 | 2 | M 2 | **4** | **HELD, not queued** — now recorded as such in `PROJECT.yaml`. Its blocker (D3) closed the same day it opened, which reads like the block clearing; it is not. D3 shipped the *question*, D2 waits on the *answer*, and zero owners have seen the prompt. Resumes on a **number**, not a date. |
| **D4** test cluster so `verify:live` can enter CI | correctness 5 | 2 | L 3 | **3.3** | Infra spend, Steve's. Two of its symptoms are now mitigated (S4 counts what gets left behind, S5 notices when the walks stop) — the *cause* is untouched. |
| **G3 wealth-manager category** should probably be deleted from the gate's `metric:` | correctness 5 | 1 | S 1 | **5** | Argued in S3; it is a wording change to a gate, so it is Steve's call, not a commit. |
| Re-verify the two Homethrive partnership dates against Homethrive's own newsroom | documentation 2 | 1 | S 1 | **2** | The May 2026 Bright Horizons item rests on a **secondary source**, flagged in the dossier. Confirm before citing it to anyone. |

## 6. Debt created

1. ~~**`verify:orphans` has never run.** The worktree has no `.env.local` by design, so the sweep is
   unit-proven and statically guarded but has **not executed against the cluster**. This is a
   *wired, not live-proven* state and is named as such — the first real run is in §7.~~
   ✅ **RESOLVED the same evening — and the run found a bug.** See §9.
2. **The freshness dead-man will fire on 2026-09-01** if `verify:live` is not run before then (the
   backfilled stamp is dated 2026-08-18 + 14 days). That is the intended behaviour, not debt in the
   usual sense, but it is a dated consequence somebody should not be surprised by.
3. **`.relay-submitter.json` exists only on this machine.** It is deliberately not in the repo, so a
   fresh clone will not have it and the value-scan layer of its guard is inert there. Stated in the
   test's own output rather than left to be discovered.

## 7. Verification the human must run — this worktree structurally could not

Gitignored files do not come with a worktree, so nothing credentialed ran here. From the **main
checkout**, after merging:

1. `npm run verify:orphans` — **the first live run of new code against production.** Read-only, but
   confirm that: it connects, the reserved-domain filter returns the accounts you expect, and the
   row census names real tables. This is the step that would have caught `standby_user_id` if the
   static guard had not.
2. `npm run verify:live` — the five walks, which now end by stamping the log. Confirm the stamp line
   appears and commit it. This also re-arms the dead-man past 2026-09-01.
3. `npm run verify:schema` — unchanged by this sprint, but it is the cheap one and nothing here has
   run against a cluster.

## 8. Recommended top 3 for the next sprint

1. **Run the two live commands above and commit the stamp** (§7). Nothing in this sprint is
   live-proven, and S4 in particular is a database script whose first real execution has not
   happened. The repo's own claim ladder puts it at *wired*, and saying otherwise would be the
   exact conflation `PROJECT.yaml` warns about.
2. **Steve's two decisions, both now dated and both cheap**: ratify or amend **G3** (`ratify_by`
   2026-09-01, and it is now mechanically enforced), naming two organisations from the dossier; and
   rule on whether **wealth manager** stays in that gate's metric.
3. **Nothing else engineering-side, and that is the point.** `demand_signal` is still `none`,
   `g1-arms-length-demand` is still at 0, and the gate is waiting on a voice pass and a cover email
   — Steve's, planned 2026-08-19 — not on code. Four of this sprint's five items made existing work
   harder to get wrong; none of them moved the only number that matters.

---

## 9. Addendum — §7 was executed the same evening, and it earned its place

> Added 2026-08-19 after Steve asked for the two live commands to be run from the main checkout.
> §6 debt item 1, §7 and §8 item 1 are all discharged by this section; the original text above is
> left standing rather than rewritten, because what it predicted is exactly what happened.

`master` was fast-forwarded to `sprint/2026-08-19-1` (a clean ancestor, no merge commit, nothing
pushed), because `verify:orphans` does not exist on the old master and there was no other way to run
it from the main checkout.

### 🔴 The first invocation failed, and the bug was worse than the one it blocked

```
$ npm run verify:orphans
'tsx' is not recognized as an internal or external command
```

`tsx` is **not a declared dependency** of this repo — not in `dependencies`, not in
`devDependencies`, not in `node_modules/.bin`. All eleven pre-existing script entries fetch it with
`npx tsx`. Both entries this sprint added used a bare `tsx`.

`verify:orphans` was merely broken. **`verify:stamp` was worse**: it is the last link of
`verify:live`, so the chain would have run all five walks — creating and deleting real accounts on
the production cluster — and then died at the final step leaving no stamp. The freshness dead-man
added by S5 would then have reported that the walks had never run, **on the day they had**. A wasted
production run *and* a lying monitor, from a missing three-character prefix.

Nothing in `gate` could see it: a string in `package.json` is not type-checked, not linted, and not
executed by any test. Fixed on `fix/tsx-invocation` (`ce1e2bf`) with a guard in `gate-script.test.ts`
that fails on a bare `tsx` while tsx is undeclared, proven by planting the bare form back.

**This is the cleanest evidence this repo has for its own claim ladder.** S4 and S5 were unit-proven,
statically guarded, four-planted-violations-verified — and still could not start. *Wired* is not
*live-proven*, and the gap between them was one `npx`.

### The sweep then proved itself on a real orphan, by accident

A `disposable-owner create` run against the **stale dev server** (see below) failed at the vault-item
step with HTTP 401 — and **left the account behind while exiting 1**. That is D4's failure mode
verbatim: *"Every run that made them REPORTED success."* The sweep found it within a minute of
existing. It was closed through `deleteAccount()` after re-checking both holds.

⚠️ **`disposable-owner create` is NOT defective** — it was retested against a healthy server and
exits 0. The 401 was the stale server, not the script. Recorded because "the a11y fixture is broken"
would have been the wrong conclusion to draw and the expensive one to chase.

### Every path proven against live data

| Path | Proof |
|---|---|
| empty cluster | `Scanned 0 reserved-domain accounts` — stated, not silent |
| finds a real orphan | `relay-a11y-…@relay.test (0.1h old) no rows` |
| row census | `vault_items=3 recipients=1 verifiers=1 access_rules=3 release_state=1 invitations=2 audit_log=3` — the half nothing had exercised |
| alarm fires | `--hours 0.001` → `FAIL: 1 disposable account(s)…`, **exit 1** |
| rejects a bad threshold | `--hours 0` → refused, exit 1 |
| `verify:live` | **exit 0** — stepup 17/17 · multiowner 14/14 · ui 26/26 · reveal 20/20 · factors 17/17 |
| the stamp | `2026-08-19T04:45:33Z @ 9f9d225` — first script-written line in the log |
| walk cleanup | every account the five walks created was gone; the sweep after them found only the orphan from the failed `create` |

**The five walks clean up after themselves properly.** That is worth stating plainly, because D4's
argument rests on an early multi-owner run that left four behind, and this run is evidence the walks
themselves are no longer the leak — the leaks come from *interrupted* fixture scripts, which is
precisely what the sweep is for.

### One thing was changed outside the repo

A **stale dev server** (PID 101896, started 19:04, before the merge) was listening on :3000, taking
16.5s to answer and returning **500**. Running the walks against it would have verified stale code —
and it is what produced the 401 above. It was stopped, `.next` cleared, and a fresh server started.
Cluster state at the end: **zero reserved-domain accounts**.

### What this addendum does NOT close

`verify-live-cannot-enter-ci` (D4) **stays open**. Two of its symptoms are now covered and its cause
is not: the walks still write to production because Relay has no dev database, and a count makes the
residue visible rather than making the writes safe. `PROJECT.yaml → deferred → D4 →
partially_mitigated` records exactly that split. The test cluster remains Steve's call.
