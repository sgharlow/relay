# Sitting sheet — ~~2026-09-12~~ **TONIGHT, 2026-09-10** (Sitting A: the real circle, and the hands-on items around it)

> **Ruled forward 2026-09-10 (Steve): "defer the hands-on items to tonight."** The filename keeps its
> date because `PROJECT.yaml` and `ROADMAP.md` point at it; the sitting is the evening of 09-10.
> Answering a `revisit: 2026-09-12` entry two days early is fine — the guard only asks that it is
> answered.

> Prepared 2026-09-02. A run sheet, not a plan: `PROJECT.yaml` and `ROADMAP.md` win where they
> disagree.
>
> **Re-baselined 2026-09-10 (ROADMAP revision 8).** Two things changed since 09-02: (1) A0's own
> revisit is now answered in the register (it was done 08-29; `verify:dogfood` READY re-measured
> 09-10), so it is no longer on the day — but (2) a **third** entry carries `revisit: 2026-09-12`
> and the 09-02 sheet missed it: `deferred.owner-mode-a11y-is-armed-by-configuration-not-required`,
> the a11y fixture owner. It is a five-minute decision and the guard goes red on 09-13 without it.
> This sitting is also **Sprint 1 of the beta plan** (ROADMAP §3, "What beta-ready means") — the
> only beta-ready condition that needs Steve's hands is item 2 below.
>
> Everything a machine could answer before the day has been answered before the day, so
> the sitting is the things only Steve can do plus the register rows they close.

## What the day is for

1. ~~**A0 — the owner's vault is filled.**~~ ✅ **Done 2026-08-29; READY re-measured 09-10.** Kept for the record — the walk was: six screens, ~20 minutes, the order the product makes
   them possible: `docs/vault-checklist-sprint-1.md`. Real login with secret + 2FA seed + recovery
   codes; a document/instruction item; **Needs a code?** answered; `/circle` one recipient AND one
   verifier; `/rules` one rule; `/triggers` saved. **Never Initiate. Never `reset-demo.ts`.**
2. **A0.2 — the REAL circle.** Two real people — one recipient, one verifier — neither a plus-alias
   or dot-variant of the owner's address. `npm run beta:status` must print no owner-alias warning
   for either afterwards (§3.7 rules 5/7; today it prints one for April and one for Ben). Invite
   circle members with `scripts/phase0-invite.ts`, **never** `invite:cohort` (the duplicate-email
   trap in `docs/beta-cohort-handoff.md`). The four-word verification call is manual by design.
3. **Answer the register in the room.** Two entries still carry `revisit: 2026-09-12` unanswered (A0.2 and B28 — A0's is answered) and the guard
   (`lib/ops/revisit-dates.test.ts`) fires the next UTC day for any without an acknowledgement key:

| Entry | What answers it |
|---|---|
| ~~`deferred.the-owners-vault-is-empty` (A0)~~ | ✅ **answered 2026-09-10** — `sprint_1_calendar_lapsed.revisit_outcome` carries the 09-10 READY measurement; nothing to do on the day |
| `deferred.adding-a-person-to-the-circle-does-not-invite-them` (A0.2) | `revisit_outcome:` — the two real names invited, `beta:status` alias-clean, and the verifier `confirmed` after the four-word call (clause a). Clause (b), the not-yet-asked roster state, is Claude's build — ROADMAP Sprint 1 row 1.3 — and is recorded when it ships, not here |
| `deferred.owner-mode-a11y-is-armed-by-configuration-not-required` (B28) | **a decision, ~5 min:** create one dedicated audit-fixture owner account (never a customer — axe prints element HTML into a public run log), then `gh secret set A11Y_OWNER_EMAIL`; Claude runs the proof-of-red dispatch and flips `a11y.yml` to fail on absence. Or move the revisit with a reason. Silence = CI red 09-13 |

Answered **before** the day, so they are not on it: A7.0 (closed 2026-09-02, the instrument is
alive on both halves) · G8 (acknowledged; ruled 2026-09-01) · C4 (ruled 2026-09-02, revisit →
2026-11-30) · A0.dm (revisited 2026-08-30) · B5's window (acknowledged 2026-09-02; closes in the
phase-B PR).

## Not on the day, and why

- **The cohort's "fourth deferral."** The register deliberately sets no fourth date: *"the honest
  next state is Sitting A, not a fourth date."* A0 is the unblock. After A0, `invite:cohort` stops
  refusing and A3 can be scheduled on its own terms — a new revisit date is written then, not now.
- **D1, the five-walk chain.** Its freshness dead-man is a timestamp (last stamp + 14 days), which
  fell at 2026-09-12T07:45Z — 00:45 PDT on the day. It was therefore run on 2026-09-02, ahead of
  the sitting; `tail -1 docs/verify-live-runs.jsonl` is the current clock. The journeys chain
  (21 days) has its own clock; `tail -1 docs/verify-journeys-runs.jsonl`.

## Pre-flight, the morning of

```bash
npm run beta:status          # both alias warnings should still be the ONLY circle rows
npm run verify:dogfood       # READY since 08-29; exit 0 confirms nothing regressed
tail -1 docs/verify-live-runs.jsonl   # D1 fresh?
```

## Also tonight, by the same ruling — each is minutes and each is on the beta-ready list

- **`stripe login`** — both CLI session keys expire 2026-10-07 (ROADMAP Sprint 1 row 1.8).
- **`chrome://settings/security`** — is Enhanced protection ON? `/vault/new` loaded clean in this
  Chrome on 09-10; that one read closes the Safe Browsing incident entry (row 1.9).
- **The cohort roster** (`.relay-cohort.json`, row 1.7) — if it exists by 09-17, Sprint 2 has people
  in it. Outlook addresses go on the owner arm (Resend, 09-03: Junk by domain reputation, no remedy).
- **Do not sign in as the owner between 09-17 and 09-21** — the first reminder this product has
  ever sent fires 2026-09-21 15:49Z (`npm run check:ladder`), and a sign-in moves it.

## Finish line

`verify:dogfood` exit 0 · `beta:status` alias-clean with two real people invited · the A0.2 and
B28 rows above carry `revisit_outcome:` (or a moved date with a reason) · `TZ=UTC npx vitest run
lib/ops/revisit-dates.test.ts` green on the day and the day after.
